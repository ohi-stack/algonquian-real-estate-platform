# Plugin Source Import Instructions

Extract each package into its own versioned directory under `plugin-packages/`, preserving plugin structure and assets. Then verify expected files, run PHP/WordPress checks, scan for secrets, reconcile versions, verify canonical `deal_id` and service/event contracts, run synthetic cross-plugin tests, and only then update `STATUS.md`.
